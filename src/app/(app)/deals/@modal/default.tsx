// Default (unmatched) state for the @modal parallel slot: render nothing so the
// deals list shows normally when no deal is being intercepted.
export default function ModalDefault() {
  return null;
}
